server:
  port: 8080

spring:
  datasource:
    url: jdbc:sqlite:hotel.db
    driver-class-name: org.sqlite.JDBC
  security:
    user:
      name: admin
      password: hotel123
  sql:
    init:
      mode: never
      #schema-locations: classpath:schema.sql

logging:
  level:
    com.hotel.server: DEBUG           # Твои классы
    org.springframework.web: DEBUG    # HTTP запросы
    org.springframework.security: DEBUG # Авторизация
  pattern:
    console "%d{HH:mm:ss} %-5level [%c{1}] - %msg%n"